# All in 1 Store — متجر إلكتروني عصري وحديث 🚀

متجر إلكتروني **كامل بدون باك إند** — يعمل بالكامل في المتصفح مع واجهة احترافية، لوحة إدارة متقدمة، وتكامل الذكاء الاصطناعي.

## ✨ الميزات الرئيسية

✅ لوجو جميل + عنوان نيون مركزي  
✅ عربة تسوق دائمة في الجانب  
✅ حساب المستخدم مع عرض البيانات  
✅ بحث وترتيب سريع للمنتجات  
✅ **فئات منتجات احترافية** (الكترونيات، صحة، إلخ)  
✅ **شارات منتجات ذكية** (مميز ⭐ | خصم 🏷️ | شحن مجاني 🚚)  
✅ **موديال منتج تفصيلي** مع صور متعددة وتفاصيل  
✅ **تحميل/كشط آلي** لوصف المنتج بـ Gemini  
✅ لوحة إدمن احترافية (email/password/phone)  
✅ استيراد من Excel/CSV  
✅ **نشر تلقائي على وسائل التواصل** 📱 (فيسبوك، تيليجرام، إنستجرام)  
✅ إحصائيات وتقارير  
✅ واتساب Checkout مباشر  
✅ أيقونات وإيموشنز في كل مكان

الميزات المضمنة:
- واجهة عصرية مع عنوان نيون مركزي.
- استيراد منتجات من ملف CSV/XLSX (قالب في `products_template.csv`).
- لوحة ادمن محلية بـ PIN (افتراضي `1234`) لإضافة/تعديل/حذف منتجات.
- حفظ المنتجات والحسابات والعربة والاحصائيات في `localStorage` (لا خادم).
- عربة تسوق، حساب عميل يحفظ الاسم، الهاتف والعنوان.
- عند الضغط على "شراء الآن" تفتح نافذة واتساب جاهزة ترسل الرسالة إلى رقمك: `01284731863` (أضفتها بصيغة دولية للواستخدام: `+20` داخل الرابط).
- توليد وصف آلي (محلي احتياطي) وخيار لصق مفتاح Gemini لطلب وصف من API (تحذير: وضع المفتاح في المتصفح يعرضه للخطر وقد يفشل بسبب CORS).
- احصائيات محلية للزيارات والطلبات (مخزنة محليًا).

ملاحظة مهمة حول "بدون باك اند":
- هذا التطبيق يعمل بالكامل على المتصفح ويخزّن كل شيء محليًا. لذلك البيانات (المنتجات، الطلبات، احصائيات) ستكون محلية على متصفح كل مستخدم ولا تتزامن بين أجهزة أو زوار آخرين.
- لعمل نظام ادارة مركزي وذكاء اصطناعي حقيقي آمن (Gemini) وanalytics حقيقي، تحتاج لباك اند وسيط آمن لاستقبال الطلبات، تخزينها، والاتصال بمفتاح Gemini بعيدًا عن متصفح الزوار.

تشغيل محلي سريع:
1. افتح `index.html` مباشرة في المتصفح (أفضل تشغيل عبر سيرفر محلي لعمل بعض السياسات: مثلاً `npx serve` أو فتح داخل مشروع Vite).

استيراد المنتجات:
- افتح `products_template.csv` في Excel وعدّل الخلايا ثم احفظ بصيغة CSV أو XLSX.
- من لوحة الادمن اختر الملف ثم اضغط "استيراد من ملف".

إرشادات أسماء صور المنتجات (تنسيق مطلوب):
- ضع صور المنتجات في المجلد `assets/products`.
- سُمّ الملفات بصيغة `p{رقم_المنتج}-{رقم_الصورة}.{ext}` مثال: `p1-1.png`, `p1-2.jpg`, `p2-1.png`.
- يدعم المتجر صيغ `png`, `jpg`, `jpeg`, `svg`.
- لكل منتج يمكن أن ترفع حتى 5 صور بنفس نمط التسمية (p1-1 .. p1-5).
لوجو الموقع:
- ضع شعار الموقع باسم `icon-b.png` في جذر المشروع أو في `assets/` إذا رغبت. الملف الحالي المستخدم في الهيدر هو `icon-b.png`.


المراد لاحقًا (اقتراحات لتحسينات):
- استخدام باك اند بسيط (Node/Express/Firebase) لتخزين المنتجات والطلبات وسرية مفاتيح Gemini.
- ربط Google Analytics أو حلول أخرى للحصول على احصائيات حقيقية عبر المستخدمين.

---

## 📱 ميزة النشر التلقائي (جديد! 🎉)

### دعم النشر التلقائي على:
- ✅ **فيسبوك** — نشر مع الصور تلقائياً
- ✅ **تيليجرام** — نشر على القنوات والمجموعات
- ✅ **إنستجرام** — مشاركة يدوية أو متقدمة

### كيفية الاستخدام:
1. من لوحة الإدارة، اذهب إلى **"⚙️ إعدادات وسائل التواصل"**
2. أدخل:
   - **Facebook**: Page ID و Page Access Token
   - **Telegram**: Bot Token و Chat ID
3. احفظ البيانات
4. اضغط على أي زر نشر بجانب المنتج (📣 فيس | ✈️ تيليج | 📸 إنستا)
5. عدّل النص إذا أردت
6. اضغط "✈️ نشر الآن"

📖 **للتفاصيل الكاملة:** شاهد `SOCIAL_PUBLISHING_GUIDE.md`  
📝 **قائمة الإعداد:** شاهد `SETUP_CHECKLIST.md`
- دعم رفع صور فعلي بدلًا من روابط الصور.

إذا رغبت، أقدر: نشر المشروع لك، تحويله إلى React/Vite مع build scripts، أو إعداد باك اند بسيط آمن للـ Gemini والاحصائيات.
